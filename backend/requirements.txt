# Sistema Legal CO — Dependencias Python
# CORRECCIONES APLICADAS:
#   - python-jose → PyJWT (CVEs conocidos)
#   - paddleocr/paddlepaddle comentados → ocrmypdf + pytesseract como primario
#   - weasyprint eliminado → PDF export via LibreOffice headless (ver Dockerfile)
#   - celery + redis para batch processing
#   - boto3 para storage S3-compatible
#   - PyMuPDF en lugar de pypdf
#   - holidays para festivos colombianos
#
# NOTA CI: sentence-transformers se mueve a requirements-ml.txt
# porque arrastra torch (~2GB) y rompe el pipeline de CI.
# ChromaDB usa su embedder interno (onnxruntime) por defecto.

# ─── Web framework ───────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
websockets==13.1

# ─── Base de datos ───────────────────────────────────────────────────────────
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9

# ─── Autenticación — PyJWT (reemplaza python-jose) ───────────────────────────
PyJWT==2.10.0
passlib[bcrypt]==1.7.4
cryptography==43.0.3
# python-jose ELIMINADO — CVEs en 2023-2024

# ─── Cola de tareas ──────────────────────────────────────────────────────────
celery==5.4.0
redis==5.2.0
flower==2.0.1

# ─── LLM ─────────────────────────────────────────────────────────────────────
litellm==1.52.0
httpx==0.27.2

# ─── Vector DB ───────────────────────────────────────────────────────────────
# chromadb vuelve a 0.5.5 (versión que pasaba CI)
# Usa su embedder interno (onnxruntime, sin torch) por defecto.
# sentence-transformers movido a requirements-ml.txt (arrastra torch ~2GB)
chromadb==0.5.5

# ─── PDF / OCR ───────────────────────────────────────────────────────────────
PyMuPDF==1.24.14
pdfplumber==0.11.4
ocrmypdf==16.7.0
pytesseract==0.3.13
Pillow==10.4.0
# paddlepaddle + paddleocr COMENTADOS (imagen Docker ~4-6GB)
# Activar con OCR_ENGINE=paddleocr en .env si se necesitan documentos degradados
# paddlepaddle==2.6.2
# paddleocr==2.9.1

# ─── Word / DOCX ─────────────────────────────────────────────────────────────
python-docx==1.1.2
Jinja2==3.1.4

# ─── PDF Export — LibreOffice headless (instalado en Dockerfile como apt) ────
# weasyprint ELIMINADO: problemas con Times New Roman en Linux, CSS limitado.
# Ver app/modules/drafting/exporter_pdf.py — usa soffice --headless

# ─── NLP / NER ───────────────────────────────────────────────────────────────
spacy==3.8.2
https://github.com/explosion/spacy-models/releases/download/es_core_news_lg-3.8.0/es_core_news_lg-3.8.0-py3-none-any.whl

# ─── Rate limiting ────────────────────────────────────────────────────────────
slowapi==0.1.9

# ─── Almacenamiento S3-compatible ─────────────────────────────────────────────
boto3==1.35.80

# ─── Utilidades ───────────────────────────────────────────────────────────────
python-dotenv==1.0.1
pydantic-settings==2.5.2
python-dateutil==2.9.0
holidays==0.59
BeautifulSoup4==4.12.3
lxml==5.3.0
tenacity==9.0.0
numpy==1.26.4

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0

# ─── Dev ──────────────────────────────────────────────────────────────────────
watchfiles==0.22.0
